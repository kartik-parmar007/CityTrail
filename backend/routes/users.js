const express = require('express');
const router = express.Router();

// Placeholder for user endpoints
router.get('/', (req, res) => res.json({ message: 'Users route hit' }));

module.exports = router;
